# Matrix HRMS (Local)


This app runs locally with a mock API layer replacing external services.
It's a Vite+React app with Tailwind and Radix UI components.

## Running the app

```bash
npm install
npm run dev
```

## Building the app

```bash
npm run build
```

For local development, no external accounts or SDKs are required.